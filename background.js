chrome.runtime.onInstalled.addListener(function() {
    // chrome.history.search({text: "vk.com", maxResults: 10}, function(data) {
    //     data.forEach(function(page) {
    //         console.log(page.title + " " + page.url);
    //     });
    // });
});